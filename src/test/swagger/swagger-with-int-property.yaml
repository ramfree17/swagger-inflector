swagger: '2.0'
info:
  version: 1.0.0
  title: mock 2
  description: mock2
paths:
  /user:
    get:
      responses:
        200:
          description: OK
          schema:
            $ref: '#/definitions/User'
definitions:
  User:
    type: object
    properties:
      id:
        type: integer
        format: int64
        example: 42
      name:
        type: string
    example:
      id: 42
      name: Arthur Dent
